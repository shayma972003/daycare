# خطة التنفيذ — منصة إدارة الحضانات

> **النطاق المعتمد**: النظام الحالي بكل فيتشرزه + إصلاح كل الأخطاء + إضافة كل فيتشرز نظام ينمو + تطبيق الجوال (ولي أمر + معلمة)
> **خارج النطاق**: الفوترة الضريبية ZATCA والربط مع هيئة الزكاة — تبقى الفوترة كما هي حالياً
> **تاريخ البدء**: 1 أغسطس 2026
> **الإطلاق التجريبي**: الأسبوع 3 · **الإصدار الكامل**: الأسبوع 14

---

---

# 📍 الحالة الراهنة — آخر تحديث 1 أغسطس 2026

> **للجلسة القادمة**: اقرأ هذا القسم أولاً، ثم `git log` لتفاصيل كل إصلاح.

## ما اكتمل ✅

| المرحلة | الحالة |
|---|---|
| **المرحلة صفر — الأسبوع 1** (الأمان) | ✅ كل الثغرات الحرجة العشر مغلقة ومُتحقَّق منها |
| **بنية البيانات — المجموعة الحمراء** | ✅ Migrations · Enums · analyticsId · تشفير الهويات · سجل التصدير |
| **المرحلة صفر — الأسبوع 2** (المالية والواجهة) | ✅ مضاعفة الإيرادات · الفوترة · حدود الخطة · التوقيت |
| **الاحتفاظ بالبيانات والتجهيل (D3)** | ✅ 5 سنوات · وظيفة ليلية · سجل تدقيق · شاشة أدمن · الترحيل مُطبَّق |
| **الأسبوع 3 — الصلاحيات والموبايل** | ✅ `1.1`–`1.17` · الترحيل مُطبَّق ومُتحقَّق · ينقص مفاتيح خارجية وحضانة تجريبية |
| **الأسبوع 4-5 — تقارير الرعاية اليومية ⭐** | ✅ `2.1`–`2.8` · الترحيل مُطبَّق · ينقص رفع الصور (محجوب بـR2) |
| **الأسبوع 6 — الحضور المتقدم والفئات العمرية** | ✅ `2.9`–`2.17` (عدا `2.15` المؤجَّل بقرار) · الترحيل مُطبَّق ومُتحقَّق |
| **الأسبوع 7 — التقويم والوحدات** | ✅ `2.18`–`2.24` · الترحيل مُطبَّق · رفع ملفات الوحدات مكتمل |
| **الأسبوع 8 — الأرشفة والمناوبات والتخزين** | ✅ `2.25`–`2.31` · الترحيل مُطبَّق · استعلامات التخزين الستة مُتحقَّقة على المخطط الحيّ |
| **الأسبوع 9 — البوابة والأوصياء والتذكيرات** | ✅ `2.32`–`2.35` · `2.37`–`2.41` · ترحيل الأوصياء مُتحقَّق (21/21 · صفر تعارض) |

> **المرحلة 2 مكتملة عدا `2.42`** (استبدال ~300 لون hex) — كنس ميكانيكي بلا أثر وظيفي، مؤجَّل حتى استقرار الشاشات.
> ⚠️ الشاشات المضافة حديثاً (الرعاية · التقويم · الوحدات · المناوبات · التخزين · الصلاحيات · البوابة) نصوصها عربية حرفية بلا مفاتيح في `locales/*.json` — تحتاج استخراجاً قبل أن تعمل الإنجليزية عليها.

**76 مهمة مكتملة.** الإنتاج يعمل على `daycare-green.vercel.app`.

## البنية التقنية الحالية

```
الترحيلات      prisma/migrations — 3 ترحيلات · لا db push · migrate deploy في البناء
                scripts/migrate-deploy.mjs يعيد المحاولة ويعطّل القفل الاستشاري
                (مُجمِّع Neon لا يدعمه حتى على العنوان "المباشر")
البريد          SMTP عبر Gmail — مُختبَر ويعمل · Resend مؤجَّل حتى شراء نطاق (E1-E3)
التوقيت         src/lib/datetime.ts هو المصدر الوحيد لـ AST — لا تستخدم getFullYear محلياً
الحالات         Enums في قاعدة البيانات · النص العربي في src/lib/enum-labels.ts فقط
الأخطاء         src/lib/api-error.ts للواجهة · لا تترك await بلا try/catch
```

## المتبقي في المرحلة صفر

> **تحديث 2 أغسطس 2026**: أُغلقت `0.33` · `0.74` · `0.75` · `0.79` · `0.82`–`0.85` · `0.87`–`0.88` · `0.92`–`0.99`.
> `tsc` و `eslint` نظيفان على كل ما لُمس. أخطاء lint المتبقية سابقة في ملفات لم تُلمس (`setState` داخل effect).

- [x] **0.54** ✅ **Vitest مُضاف — 85 اختباراً، 7 ملفات، أقل من ثانية**
  `npm test` · [tests/](tests/) — دوال نقية فقط (تواريخ · احتفاظ · دورات فوترة · تقارير رعاية · حضور وسعة)
  > أول فائدة: اختبار فشل، والخطأ كان **في الاختبار لا الكود** — قارنت شريحة UTC لقيمة معناها بتوقيت الرياض
- [x] **0.97ب** ✅ `MonthlyExpense` — **الفحص قلب التوصية**: الجدول "الميت" فيه صفّان بمصاريف حقيقية، و`Expense` (الذي تقرأه التقارير) **فارغ**. مصاريف شهرين لم يرها أي تقرير. نُقلت 7 بنود (13,790 ر.س، **مطابق**) ثم أُسقط الجدول
- [x] **0.91ب** 🐛 معاملة حذف المدرسة كانت ستفشل — أُضيفت 7 جداول منذ إصلاح `0.91`: `CareReport` · `Shift` · `CalendarEvent` · `Unit` · `GuardianAccount` · `Role` (`StorageUsage` وغيرها تتتالى تلقائياً)
- [ ] **0.51** اختبار انحدار كامل يدوي — **يحتاجك**
- [ ] **0.100** اختبار انحدار شامل + مراجعة أمنية
- [x] **0.34/0.35/0.44** ✅ نقل الملفات إلى R2 — مكتمل · ينقص **تشغيل سكربت الترحيل بعد النشر**

## 🔴 قرارات بانتظار المالكة

> 📋 **الخطوات التفصيلية لكل بند إداري**: [docs/المتطلبات-الإدارية.md](docs/المتطلبات-الإدارية.md)

| # | القرار | لماذا يحجب |
|---|---|---|
| **P0** 🔴 | **السجل التجاري** | الجذر — يحجب `P1` و**M2 (البيع)** و M3/M4. غير موجود حالياً |
| **P1** | التقديم على رقم **D-U-N-S** | محجوب بـ`P0` · 5-14 يوم انتظار بعده |
| **P6** | حساب Cloudflare R2 | يحجب `0.34/0.35` ونقل الملفات قبل التطبيق |
| **D7.4** | مراجعة PDPL قانونية | قبل أي التزام تعاقدي بتقارير قطاعية |

## التالي مباشرة

**الأسبوع 3** — بوابة ولي الأمر + تقارير الرعاية اليومية → **M1 (إطلاق تجريبي)**

> الحاجز أمام اللقطات التاريخية (`D4`) **رُفع** — الأرقام المالية صحيحة ومُتحقَّق منها الآن.

---

## الافتراضات

- مطوّر واحد متفرغ + Claude Code
- 12 ساعة يومياً × 6 أيام أسبوعياً ≈ 72 ساعة/أسبوع
- الإنتاجية الفعلية بعد الإرهاق ≈ 60 ساعة/أسبوع
- إجمالي الجهد المقدَّر: **~850 ساعة ≈ 14 أسبوعاً**
- الإشعارات بالإيميل فقط (الواتساب خطوة مستقبلية)

---

## المعالم الأربعة

| المعلم | الأسبوع | ما يُسلَّم |
|---|---|---|
| **M1 — إطلاق آمن** | نهاية 3 | المنصة الحالية مُصلَحة بالكامل + إشعارات إيميل → **تشغيل تجريبي مع حضانة** |
| **M2 — تكافؤ الميزات** | نهاية 9 | كل فيتشرز ينمو على الويب + بوابة ولي الأمر → **بيع تجاري** |
| **M3 — التطبيق** | نهاية 13 | تطبيق iOS + Android + Huawei جاهز للرفع |
| **M4 — المتاجر** | نهاية 14 | التطبيق منشور |

> ⚠️ **لا تنتظري الأسبوع 14 للإطلاق.** أطلقي في M1 وابدئي بجمع الملاحظات الحقيقية.

---

## 🔴 مسارات متوازية تبدأ **اليوم الأول**

> 📋 **كل هذه البنود بخطواتها التفصيلية في [المتطلبات الإدارية](docs/المتطلبات-الإدارية.md).**

- [ ] **P0** 🔴 **السجل التجاري — الجذر، ولم يكن في الخطة**
  `P1` نفسه يحتاجه، وكذلك الفوترة النظامية والتعاقد مع الحضانات وحساب Google Play باسم الشركة.
  **يحجب M2 (البيع) و M3 و M4 (النشر)** — لا المرحلة 3 وحدها كما كان مفترضاً.
  بدونه يمكن إكمال البناء والتشغيل التجريبي (M1)، ولا يمكن البيع ولا النشر.
  ⚠️ النشاط في السجل = **برمجة/حلول تقنية** لا نشاط تعليمي (وإلا جرّ اشتراطات ترخيص لا تلزم).
- [ ] **P1** — التقديم على رقم **D-U-N-S** — **محجوب بـ`P0`** · 5-14 يوم عمل بعد التقديم
- [ ] **P2** — ⚠️ **تصحيح**: المطلوب **Apple Developer Program — Organization** (99$/سنة) لا **Enterprise** (299$/سنة). Enterprise **لا ينشر على App Store إطلاقاً** — توزيع داخلي على موظفي الشركة فقط
- [ ] **P3** — فتح حساب **Google Play Console** (25$ مرة واحدة)
- [ ] **P4** — فتح حساب **Huawei AppGallery** (مجاني)
- [~] **P5** — [مسودة سياسة الخصوصية](docs/سياسة-الخصوصية.md) ✅ مكتوبة من واقع ما ينفّذه النظام فعلاً · **ينقصها**: تعبئة بيانات الشركة · مراجعة `D7.4` · حسم النقل خارج المملكة · صياغة الشروط والأحكام والـDPA · نشرها على رابط عام
- [x] **P6** — حساب **Cloudflare R2** جاهز · Bucket `daycare-files` خاص · التوكن محدود بهذا الـBucket · مُتحقَّق منه فعلياً (list · put · get · delete) · ينقص إضافة المتغيّرات في Vercel
- [ ] **P7** — ترقية حساب **Resend** للبريد
- [ ] **P8** — تحديد **حضانتين تجريبيتين** للإطلاق في M1

---

# المرحلة 0 — الإصلاحات (الأسابيع 1-3)

> **الهدف**: منصة آمنة وحساباتها صحيحة، جاهزة للتشغيل الحقيقي.
> **قاعدة**: لا يُضاف أي فيتشر جديد في هذه المرحلة.

## الأسبوع 1 — الأمان وسلامة البيانات

### اليوم 1 — أسرار النظام والمصادقة
- [x] **0.1** حذف القيمة الافتراضية لـ `ADMIN_JWT_SECRET` وإضافة فشل عند الإقلاع — `src/lib/admin-auth.ts:5`
- [x] **0.2** إنشاء ملف تحقق من متغيرات البيئة بـ Zod يعمل عند الإقلاع (`src/lib/env.ts`) يغطي: `DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `ADMIN_JWT_SECRET`, `CRON_SECRET`, `RESEND_API_KEY`, `FROM_EMAIL`, `NEXT_PUBLIC_APP_URL`
- [x] **0.3** ربط رمز إعادة تعيين كلمة المرور بالمستخدم + عدّاد محاولات + انتهاء صلاحية أقصر — `src/app/api/auth/reset-password/route.ts:22`
- [x] **0.4** إضافة `Secure` و `__Host-` لكوكي الأدمن — `src/app/api/admin/auth/login/route.ts:41`
- [x] **0.5** التحقق من ادعاء `role` في `verifyAdminSession` + التأكد أن السوبر أدمن ما زال موجوداً — `src/lib/admin-auth.ts:14`
- [x] **0.6** إصلاح تحقق التسجيل ليفحص `User.email` لا `School.email` + وضعه داخل transaction — `src/app/api/auth/register/route.ts:37`

### اليوم 2 — تأمين المسارات العامة و Rate Limiting
- [x] **0.7** استبدال `school_id` في مسار الحضور العام بتوكن موقّع قابل للتدوير وله انتهاء صلاحية — `src/app/api/attendance/public/[school_id]/route.ts`
- [x] **0.8** إضافة فلتر `checkoutAt: null` لمسار الانصراف العام لمنع التكرار — `src/app/api/attendance/public/checkout/route.ts:29`
- [x] **0.9** إضافة `logAction` لكل مسارات الحضور العامة
- [x] **0.10** بناء طبقة **Rate Limiting** (Upstash Redis أو جدول في قاعدة البيانات) وتطبيقها على: تسجيل الدخول، التسجيل، نسيت كلمة المرور، إعادة التعيين، 2FA، إعادة إرسال OTP، التسجيل الذاتي، رفع الملفات
- [x] **0.11** إضافة قفل الحساب بعد 5 محاولات فاشلة + مقارنة bcrypt وهمية لمنع تسريب وجود المستخدم — `src/lib/auth.ts:81`

### اليوم 3 — التحول الكامل للإيميل
- [x] **0.12** إضافة دالة تهريب HTML وتطبيقها على كل متغيرات قالب الإيميل — `src/lib/notifications.ts:73`
- [x] **0.13** تحويل **2FA** من واتساب إلى إيميل — `src/lib/auth.ts:104`
- [x] **0.14** تحويل **OTP التسجيل الذاتي** إلى إيميل + تشفيره بـ bcrypt + عدّاد محاولات (5) — `src/app/api/enrollment/verify-otp/route.ts:40`
- [x] **0.15** تحويل **إعادة تعيين كلمة المرور** إلى إيميل فقط
- [x] **0.16** تحويل **تذكيرات الدفع** و**إشعارات الأنشطة** إلى إيميل
- [x] **0.17** إبقاء `sendWhatsApp` كما هي لكن خلف flag `ENABLE_WHATSAPP=false`
- [x] **0.18** إصلاح throttle إعادة إرسال OTP وإبطال الجلسات السابقة — `src/app/api/auth/resend-2fa-otp/route.ts:28`
- [x] **0.19** استبدال `Math.random()` بـ `crypto.randomInt` في كل توليد OTP وكلمات المرور المؤقتة
- [x] **0.20** استبدال `cuid()` بـ `crypto.randomBytes` لتوكنات التسجيل — `prisma/schema.prisma:612`

### اليوم 4 — سلامة البيانات المالية
- [x] **0.21** 🔥 إصلاح `generatePaymentCycles` ليحافظ على الدورات المدفوعة بدل حذف الكل — `src/lib/payment-cycles.ts:30`
- [x] **0.22** إصلاح انقلاب الشهر (31 يناير → 3 مارس) في توليد الدورات — `src/lib/payment-cycles.ts:26`
- [x] **0.23** وضع الحذف والإنشاء داخل `$transaction` + حد أقصى لعدد الدورات
- [x] **0.24** إصلاح `cleanupExpiredTrash` بإضافة معالجة `PaymentCycle` + try/catch لكل عنصر + تحديد دفعات — `src/lib/trash-cleanup.ts:11`
- [x] **0.25** إصلاح مصادقة الـ Cron لتستخدم `Authorization: Bearer` بدل `x-cron-secret` — `src/app/api/admin/cron/*/route.ts`
- [x] **0.26** جدولة `cron/alerts` في `vercel.json` + إضافة `maxDuration`
- [x] **0.27** استخدام `crypto.timingSafeEqual` لمقارنة سر الـ Cron

### اليوم 5 — الصلاحيات وعزل المستأجرين
- [x] **0.28** إضافة `schoolId` إلزامي مع فشل صريح في `requireSession()` — `src/lib/session.ts:19`
- [x] **0.29** تحويل `requireSession` لإرجاع نتيجة نوعية بدل رمي `Error` عام
- [x] **0.30** التحقق من ملكية `guardianId` / `classId` / `teacherId` قبل أي كتابة — `students/route.ts:140`, `students/[id]/route.ts:161`, `classes/route.ts:88`
- [x] **0.31** التحقق من ملكية `class_id` عند اعتماد طلب التسجيل — `enrollment/approve/[submission_id]/route.ts:118`
- [x] **0.32** إضافة `deletedAt: null` لـ `bulk-status` + استخدام enum بدل نص حر — `students/bulk-status/route.ts:34`
- [x] **0.33** إضافة `schoolId` للاستعلامات الثانوية (دفاع بالعمق) في `teachers/[id]`, `classes/[id]`, `bulk-extend`, `notifications/alerts` · وأثناءها: سقف 500 على `bulk-extend` · تاريخ هجري في سجل التمديد (`toLocaleDateString("ar-SA")`) · حدود شهر بتوقيت الخادم في عدّاد تأخير المعلمة

### اليوم 6 — الملفات والفهارس
- [x] **0.34** Cloudflare R2 + [`r2.ts`](src/lib/r2.ts) و[`storeUpload`](src/lib/file-upload.ts) — **الدلو خاص** (Public Access معطَّل): صور أطفال ومسوحات هوية، ورابط عام مسرَّب يبقى مقروءاً للأبد بلا جلسة تُلغى. القراءة عبر [`/api/files`](<src/app/api/files/[...key]/route.ts>) ← رابط موقَّع 5 دقائق، والبايتات تمر من Cloudflare للمتصفح مباشرة لا عبر دالتنا. **المستأجر أول مقطع في المفتاح** فيُفحَص بلا استعلام، والمقطع الأخير UUID فلا يُشتق مفتاح من معرّف طفل
- [x] **0.35** [سكربت الترحيل](scripts/migrate-files-to-r2.mjs) — يلمس فقط ما يبدأ بـ`data:` فتكراره آمن · الترتيب: رفع ← تسجيل ← تحديث العمود، فالانهيار يترك بايتاً ضائعاً لا صفّاً يشير إلى ملف غير موجود · **`Invoice.pdfUrl` لم يُنقل عمداً**: إعادة تصيير لأرقام بجانبه، وزر الإفراغ موجود أصلاً · ⏸️ **يُشغَّل بعد النشر** — الإنتاج الحالي لا يعرف `/api/files`
- [x] **0.36** إصلاح أو تعطيل `/api/upload` المكسور — `src/app/api/upload/route.ts:39`
- [x] **0.37** حدود حجم واقعية (صورة 5MB، ملف 10MB) + فحص magic bytes لا `file.type`
- [x] **0.38** حدود حجم وعدد صفوف على رفع Excel + ترقية `xlsx` أو استبدالها بـ `exceljs`
- [x] **0.39** إضافة الفهارس الناقصة: `Student.schoolId`, `Teacher.schoolId`, `Class.schoolId`, `Guardian.schoolId`, `Attendance(schoolId,date)`, `Attendance(studentId,date)`, `TeacherAttendance(schoolId,date)`, `Invoice.schoolId`, `NotificationLog.schoolId`, `Expense.school_id`, `TwoFASession.bypassTokenHash`, `EnrollmentSubmission.school_id`
- [x] **0.40** إضافة `@@unique([studentId, date])` و `@@unique([teacherId, date])` على جداول الحضور
- [x] **0.41** تحويل كل مسارات تسجيل الحضور من `findFirst`+`create` إلى `upsert`
- [x] **0.42** جعل كل عمليات الانصراف idempotent (فلتر `checkoutAt: null`) + داخل `$transaction`

### اليوم 7 — التصلّب والبذور واحتياطي
- [x] **0.43** إضافة رؤوس الأمان في `next.config.ts`: CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy
- [x] **0.44** ~~`images.remotePatterns`~~ **غير مطلوب**: العمود يحمل مساراً على نطاقنا (`/api/files/…`) لا رابط R2، فلا شيء يُحمَّل من أصل خارجي. المطلوب فعلياً كان `img-src` في CSP — لأن التحويل 302 نحو الدلو يُفحَص من جديد عند تجاوز الأصل
- [x] **0.45** إصلاح `prisma/seed.ts` المنهار (حقول `guardianName1`/`phone1` غير موجودة) + جعله idempotent
- [x] **0.46** إصلاح `seed-admin.ts`: إلزام متغيرات البيئة، تدوير كلمة المرور فعلياً، الخروج بكود خطأ عند الفشل
- [x] **0.47** إضافة `db:seed:admin` لـ `package.json`
- [x] **0.48** إزالة تسريب تفاصيل الأخطاء (`String(error)`, `error.stack`) من كل الاستجابات
- [x] **0.49** إزالة `console.log` لبيانات PII من مسارات الفواتير
- [x] **0.50** إضافة `TwoFASession` و `ImportSession` لوظيفة التنظيف الليلية
- [ ] **0.51** ✅ **اختبار انحدار كامل** + إصلاح ما ينكشف

---

## الأسبوع 2 — صحة الأرقام والواجهة

### اليوم 8 — المحرك المالي (1)
- [x] **0.52** 🔥 إصلاح مضاعفة الشهر: توحيد حساب المدى والمكوّنات على AST — `src/lib/finance-calculator.ts:18` + `src/lib/finance.ts:35`
- [x] **0.53** إعادة كتابة `getPreviousPeriodRange` بالكامل (خاطئ للشهري والسنوي ونصف السنوي H1) — `src/lib/finance.ts:39`
- [x] **0.54** [اختبارات المدى الزمني](tests/finance-periods.test.ts) — 10 اختبارات تثبّت الحدود **بتوقيت الرياض**: الخطأ السابق كان قراءة مكوّنات التقويم من لحظة مُزاحة بـ`getUTC*` (أي 21:00 من اليوم السابق)، فقارن السنوي بسنتين للخلف والشهري بشريحة يومين من شهر خاطئ — وكلها أعطت نسب نمو تبدو معقولة

### اليوم 9 — المحرك المالي (2)
- [x] **0.55** فصل الضريبة عن الإيراد — تُسجَّل كالتزام لا كدخل — `src/lib/finance.ts:238`
- [x] **0.56** ربط احتساب الضريبة بـ `School.vatRegistered` بدل 15% ثابتة للجميع
- [x] **0.57** حسم ازدواجية `registration_fee`: إما رسم تسجيل لمرة واحدة أو رسم شهري — لا الاثنين — `finance.ts:225,237`
- [x] **0.58** توحيد معادلة المقارنة بين الفترة الحالية والسابقة — `finance.ts:238` مقابل `:283`
- [x] **0.59** إرجاع الطلاب `SUSPENDED` و `CANCELLED` إلى حسابات التحصيل والمستحقات — `finance.ts:145`
- [x] **0.60** معالجة الطلاب بلا `enrollmentEndDate` (التسجيل المفتوح) بدل استبعادهم — `finance.ts:197`
- [x] **0.61** إزالة `salaries.paid` الملفّق واستبداله بمصدر حقيقي أو إخفاء الحقل — `finance.ts:341`
- [x] **0.62** إخراج `deactivateExpiredExpenses` من مسار التقارير (قراءة لا تكتب) — `finance.ts:189`
- [x] **0.63** تحويل `deactivateExpiredExpenses` إلى `updateMany` واحدة — `expense-updater.ts:11`

### اليوم 10 — التوقيت والتواريخ
- [x] **0.64** إنشاء `src/lib/datetime.ts` كمصدر وحيد لكل حسابات AST (UTC+3)
- [x] **0.65** استبدال كل `new Date(y,m,d)` و `setHours()` بدوال AST الموحّدة في: `payment-status-updater.ts:15`, `attendance-data.ts:25`, `attendance/public/*`, `students/[id]/checkin|checkout`
- [x] **0.66** إصلاح القيمة السالبة في `setUTCHours(h - 3)` — `attendance/students/checkout/route.ts:81`
- [x] **0.67** استبدال وقت 17:00 الثابت بـ `School.studentCheckoutTime` — `attendance/public/checkout/route.ts:34`
- [x] **0.68** إضافة `timeZone: "Asia/Riyadh"` و `calendar: "gregory"` لكل `toLocaleDateString/TimeString` — `utils.ts:31,40`, `message-variables.ts:26`
- [x] **0.69** إصلاح `end_date: { lt: now }` لتستخدم حدود AST — `expense-updater.ts:7`

### اليوم 11 — الفوترة (تصحيح فقط، بدون ZATCA)
- [x] **0.70** إضافة `@@unique([schoolId, invoiceNumber])` + توليد الرقم داخل `$transaction` بدل `count()+1`
- [x] **0.71** نقل حساب كل الإجماليات للخادم — رفض أي `grandTotal`/`vatAmount` قادم من العميل — `invoices/generate/route.ts:163`
- [x] **0.72** نفس الإصلاح لفاتورة الراتب — `invoices/generate/teacher/route.ts:143`
- [x] **0.73** نفس الإصلاح لفاتورة الأدمن + **إصلاح إهمال الكمية** — `AdminInvoiceModal.tsx:47`
- [x] **0.74** كتابة `vat_amount` فعلياً — كان يُكتب في مسار واحد من خمسة · الرسوم تُعامل كشاملة للضريبة فتُستخرج منها لا تُضاف فوقها · فواتير الرواتب صفر **صراحةً** (الراتب ليس توريداً خاضعاً)
- [x] **0.75** منع تكرار الفاتورة لنفس الطالب/المعلمة في نفس الشهر — `src/lib/invoice-duplicates.ts` · يرجع 409 مع معرّف الفاتورة القائمة · `?force=1` لإعادة الإصدار المتعمّدة
- [x] **0.76** إصلاح `teacher.lateHours` التراكمية التي تُخصم في كل فاتورة راتب — `teachers/[id]/invoice/route.ts:27`
- [x] **0.77** مزامنة `late-fee` بين `lateHours` وسجلات الحضور — `students/[id]/late-fee/route.ts:29`

### اليوم 12 — الواجهة (1)
- [x] **0.78** إضافة `.catch()` ومعالجة أخطاء لكل الطلبات غير المُعالجة (~20 موضعاً في لوحة الأدمن + `students/page.tsx:221`, `students/[id]:275,391`, `AttendanceBoard.tsx:35`)
- [x] **0.79** إصلاح تجمّد صفحة الإحصائيات — `.finally` كان يُنهي التحميل بينما `summary` يبقى `null`، والشرط يعرض الدوّار للأبد. الآن حالة خطأ صريحة + زر إعادة محاولة
- [x] **0.80** إصلاح "إرسال تذكير" الوهمي للمعلمة (يعرض alert بلا استدعاء API) — `teachers/[id]/page.tsx:168`
- [x] **0.81** إصلاح `classId` الذي لا يُحفظ عند تعديل المعلمة — `teachers/[id]/page.tsx:132`
- [x] **0.82** `registration_fee` يُحذف من الطلب بدل إرساله `0` عند الافتراضي — الصفر يعني "لا رسوم" في طبقة المالية، فكان الطفل يُنشأ برسوم تسجيل صفر بينما النموذج يعرض رقماً من الإعدادات · وأوقف إعادة توليد دورات الدفع عند كل حفظ للملف
- [x] **0.83** الإشعار عند تعديل نشاط صار **اختيارياً بمربع تأكيد** — كان كل تعديل يعيد مراسلة كل أولياء أمور الفصول المدعوّة
- [x] **0.84** تحميل النشاط كاملاً عند التعديل (`GET /api/activities/[id]`) — الشبكة لا تحمل `teacherId` ولا الفصول، فكان الحفظ يكتب الفراغ ويلغي المعلمة وكل الدعوات · وأثناءها: `fee` كان يُتجاهل في `PUT`، و`teacherId`/`classIds` تُكتب **بلا تحقق ملكية** (فصول مستأجر آخر تُدعى وتُشعَر)
- [x] **0.85** حالة فشل صريحة في معالج الاستيراد مع إعادة محاولة/رجوع/إنهاء — كان الدوّار يدور تحت "لا تغلق الصفحة" بلا أي زر
- [x] **0.86** إصلاح تصدير CSV (تهريب الفواصل + تصدير كل الصفحات) — `admin/logs/page.tsx:66`

### اليوم 13 — الواجهة (2) والاشتراكات
- [x] **0.87** القوائم تُولَّد من الـenum في كل الشاشات — الفلتر والإجراء الجماعي كانا يرسلان `"بانتظار الدفع"` نصاً: الفلتر لا يطابق شيئاً، والإجراء الجماعي يُرفض 422 ويبتلعه `.catch(() => {})` فيبدو ناجحاً · و`PaymentStatusBadge` لم يكن فيه مفتاح `PENDING` أصلاً (أشيع حالة تُعرض بنقطة رمادية ومفتاح غير مترجم)
- [x] **0.88** القيمة الافتراضية `PENDING` — متحقَّق منها في `schema.prisma`
- [x] **0.89** 🔥 **تطبيق `subscription_status` فعلياً**: منع دخول المدرسة المعلّقة/المنتهية — `src/lib/auth.ts:76`
- [x] **0.90** تطبيق حدود الخطة (`max_students`, `max_classes`) عند الإنشاء لا كتنبيه فقط
- [x] **0.91** إصلاح transaction حذف المدرسة (8 جداول ناقصة: PaymentCycle, ActivityLog, AdminInvoice, TwoFASession, Expense, FinancialReport, EnrollmentToken, EnrollmentSubmission) — `admin/schools/[id]/route.ts:191`
- [x] **0.92** `notifications/alerts` صار POST — كـGET كان أي prefetch أو كاش يختم `payment_notified_at` ويكتم تنبيهاً لم يره أحد. **مُتحقَّق: GET يرجع 405 الآن**
- [x] **0.93** ترقيم صفحات السجلات محمي — `parseInt("abc")` = NaN و`Math.max(1, NaN)` = NaN فيرفضه Prisma بـ500 · و`limit=0` كان يعطي صفحة فارغة تبدو كـ"لا سجلات" · والتواريخ غير الصالحة تُتجاهل بدل أن تكون قاتلة
- [x] **0.94** سياسة موحّدة `src/lib/password-policy.ts` — 8 أحرف + cost 12. تغيير كلمة مرور المستأجر كان 6 أحرف و cost 10، وهو أكثر مسار يُستخدم فعلياً
- [x] **0.95** `School.email` و `User.email` يُكتبان معاً داخل معاملة — البريد المعروض كان ينفصل عن بريد الدخول، فتذهب استعادة كلمة المرور والـ2FA للقديم · يُرفض 409 عند تعدّد المستخدمين أو تعارض البريد
- [x] **0.96** دمج أولياء الأمور صار **ربطاً فقط** عند مطابقة عائلة أخرى — كانت الكتابة تستبدل اسم وبيانات ولي أمر آخر ببيانات هذا الطفل وتُعيد نسبة كل إخوته

### اليوم 14 — إغلاق المرحلة
- [x] **0.97** حُذف: `ui/Badge`, `ui/Button`, `ui/Card`, `ui/Input`, `AddStudentModal`, `AddTeacherModal`, `ImagePlaceholder`, `replaceMessageVariables`, `getSchoolId`
  - ⏸️ **`MonthlyExpense` لم يُحذف**: حذف النموذج = ترحيل يُسقط جدولاً قد يحمل مصاريف تاريخية. قرار منفصل يحتاج موافقتك
  - ⏸️ **مسار XLSX**: لا يوجد مسار معطّل — `xlsx` مستخدمة فعلياً في 4 مسارات (استيراد وتصدير)
- [x] **0.98** أُزيلت `@tanstack/react-query` و `@fontsource/noto-sans-arabic` · وأُزيل `@import` خط Tajawal المكرر من `globals.css` (كان يُحمَّل مرتين: `next/font` + طلب حاجب من Google). **مُتحقَّق في المتصفح: 0 طلبات لـ googleapis والخط ما زال مطبَّقاً**
- [x] **0.99** لا وجود لـ`NEXT_PUBLIC_FIREBASE_*` ولا `NEXT_PUBLIC_APP_NAME` في الكود ولا `.env` ولا `.env.example`
- [ ] **0.100** ✅ **اختبار انحدار شامل + مراجعة أمنية**

---

## الأسبوع 3 — الأساس التقني والإطلاق التجريبي

### اليوم 15-16 — حسابات المستخدمين ونظام الصلاحيات ✅
- [x] **1.1** حسابات: `GuardianAccount` جديد · **حساب الموظف بُني على `User` القائم لا كجدول ثانٍ** — جدول اعتماد موازٍ يعني مسارَي دخول ومسارَي استعادة وعدّادَي قفل لنفس الجمهور، وكل تحصين الأسبوع الأول (تحديد المعدل · تسوية التوقيت · فحص الاشتراك) كان سيُكرَّر أو لا يسري على النسخة. الناقص لم يكن جدولاً بل **دوراً**
- [x] **1.2** `Role` بمصفوفة `permissions` · **مفاتيح الصلاحيات في الكود لا في جدول** ([permissions.ts](src/lib/permissions.ts)) — المفاتيح خاصية إصدار لا بيانات؛ جدول يعني ترحيل بيانات مع كل نشر، وصفّاً منحرفاً عن الكود = صلاحية تمنحها الواجهة ولا يفرضها شيء
- [x] **1.3** الأدوار الستة تُزرع لكل مدرسة (لا عالمياً — الحضانة تعدّل صلاحيات معلماتها دون أن يتغيّر ذلك لبقية المستأجرين) · **مُتحقَّق: 3 مدارس × 6 = 18 دوراً، وكل مالك يحمل `manager`**
- [x] **1.4** [`proxy.ts`](src/proxy.ts) (كان `middleware.ts` — Next 16 أعاد تسمية الاصطلاح وأصدر تحذير إهمال، فنُقل) — لم يكن موجوداً رغم توثيقه في CLAUDE.md؛ كل صفحات اللوحة كانت تُخدَم لمن يكتب الرابط. **مبني على المنع بالافتراضي**: كل شيء محمي إلا استثناءات صريحة، فنسيان مسار = صفحة تطلب دخولاً (مرئي فوراً) لا صفحة مكشوفة
- [x] **1.5** جدول [route-permissions.ts](src/lib/route-permissions.ts) يغطي **كل** المسارات، يُفرَض في `requireSession()` التي تستدعيها كل المسارات أصلاً — بدل لصق فحص في 128 ملفاً حيث الغياب غير مرئي. مسار غير مصنَّف يُرفض بالافتراضي
- [x] **1.5b** إعادة التحقق في كل طلب: الحساب موجود · غير معطَّل · نفس المستأجر · الاشتراك حيّ. **موظف محذوف كان يحتفظ بجلسة صالحة حتى انتهاء التوكن**
- [x] **1.5c** 105 كتلة `catch` في 85 ملفاً أُعيدت كتابتها آلياً لتُرجع **403** بدل تسطيحها إلى 401 — العميل يعامل 401 كـ"انتهت جلستك" فيقذف المعلمة لصفحة الدخول كلما لمست ما لا تملكه. **مُتحقَّق: 91/91 مساراً**
- [x] **1.6** [شاشة الصلاحيات](<src/app/(dashboard)/settings/permissions/page.tsx>) — جزآن: مَن يدخل، وماذا يسمح كل دور. الدمج في شبكة لكل شخص هو التصميم البديهي والخاطئ: الصلاحيات تُضبط 12 مرة لـ12 معلمة ثم تنحرف، وهي المشكلة التي وُجدت الأدوار لمنعها

### اليوم 17-18 — مصادقة الموبايل والبنية التحتية ✅
- [x] **1.7** [mobile-auth.ts](src/lib/mobile-auth.ts) — توكن وصول قصير (15د، بلا قراءة قاعدة بيانات) + Refresh مبهم **مخزَّن مُجزَّأً** (تسريب الجدول يجب ألا يكون مجموعة جلسات عاملة). **يدور مع كل استخدام** ومعه `familyId`: تقديم توكن سبق تدويره = نسخة في التداول ⇒ تُلغى السلسلة كاملة. كشف السرقة هو كل ما يستطيعه اعتماد طويل العمر
- [x] **1.8** دخول برقم الجوال + رمز على **البريد** — الجوال يعرّف والرمز يصل للبريد (SMS يحتاج بوابة غير موجودة، البريد موصول ومجاني). **نفس الرد سواء كان الرقم مسجَّلاً أم لا** وإلا صار المسار دليل عائلات الحضانة
- [x] **1.9** دعوة ولي الأمر — الحضانة تدعو، ولا تسجيل ذاتي: الوالد لا "يطالب" بطفل. التوكن **مُجزَّأ** كتوكن استعادة كلمة المرور. أول دخول ناجح = قبول الدعوة (من أثبت تحكمه بالبريد أدّى ما تطلبه الدعوة)
- [x] **1.10** [push.ts](src/lib/push.ts) — `DeviceToken` + طابور + إعادة محاولة متدرّجة + **تقاعد التوكن عند رفض دائم**. مُطابَر لا فوري: نقرة "تسجيل حضور" يجب ألا تنتظر طرفاً ثالثاً ولا تفشل بفشله
- [x] **1.11** HMS منفصل — **أجهزة Huawei بلا Google Play Services**: FCM يقبل الرسالة ولا تصل أبداً، بلا خطأ ولا إعادة محاولة. فشل صامت يبدو كتكامل سليم، ولذلك `HUAWEI` منصة مستقلة لا نكهة من Android
- [x] **1.12** `/api/mobile/v1/*` منفصل ومُرقَّم — التطبيق في الأيدي لا يُحدَّث عند الطلب، فمساراته يجب أن تثبت بينما مسارات الويب تتغير. [guardianChildIds](src/lib/mobile-guard.ts) نقطة واحدة لقاعدة "ولي الأمر يرى أطفاله فقط"
- [x] **1.13** [PasswordRules](src/components/ui/PasswordRules.tsx) — 5 شروط حيّة في التسجيل والاستعادة والإعدادات وإنشاء حسابات الموظفين. الشرط الإلزامي وحده يحجب؛ فرض الرمز يُنتج `Password1!`

### اليوم 19-20 — تجهيز الإطلاق التجريبي
- [x] **1.14** بذور تُنشئ الأدوار قبل المالك (لا اعتماد على مسار الاحتياط)
- [x] **1.15** [monitoring.ts](src/lib/monitoring.ts) + [`/api/health`](src/app/api/health/route.ts) — طبقة رفيعة لا SDK: المزوّد غير محسوم، والمهم أن يكون هناك **دالة واحدة** يستدعيها كل مسار خطأ ⇒ التحويل إلى Sentry تعديل في ملف لا مطاردة عبر مئة `console.error`
- [x] **1.16** [BACKUP_AND_RESTORE.md](docs/BACKUP_AND_RESTORE.md) — الإجراء وخطة الاستجابة للحوادث · ⚠️ **التفعيل يحتاجك**
- [x] **1.17** [دليل الاستخدام](docs/دليل-الاستخدام.md) للحضانة
- [ ] **1.14ب** اختبار حِمل — **محجوب: لا يوجد إطار اختبارات** (نفس حاجز `0.51`/`0.54`)

#### 🔴 خطوات مطلوبة منكِ (لا يمكن للكود تنفيذها)
- [ ] **1.15أ** ضبط `ERROR_WEBHOOK_URL` في Vercel — الكود جاهز وينتظره
- [ ] **1.16أ** رفع History retention في Neon · أول `pg_dump` خارجي · **أول اختبار استعادة موثَّق قبل M1**
- [ ] **1.16ب** مراقبة خارجية تنادي `/api/health` (UptimeRobot مجاني)
- [ ] **1.10أ** مفاتيح Firebase (`FCM_SERVER_KEY` · `FCM_PROJECT_ID`) — بدونها الطابور يعمل ولا يُرسل
- [ ] **1.11أ** حساب Huawei (`HMS_APP_ID` · `HMS_ACCESS_TOKEN`) — مرتبط بـ`P4`

### اليوم 21 — 🚀 **M1: الإطلاق التجريبي**
- [ ] **1.18** تشغيل حضانة تجريبية واحدة + مراقبة يومية — **يحتاجك (`P8`)**
- [ ] **1.19** قناة ملاحظات مباشرة مع الحضانة — **يحتاجك**

---

# المرحلة 2 — فيتشرز ينمو على الويب (الأسابيع 4-9)

## الأسبوع 4-5 — تقارير الرعاية اليومية ⭐ (أهم فيتشر)

- [x] **2.1** نموذج `CareReport` — 8 أنواع، **أعمدة مُصنَّفة لا JSON**: "كم أكل هذا الأسبوع" و"كم نام" و"تدرّج الحرارة" هي أسئلة الفيتشر نفسه، وJSON لا يُفهرَس ولا يُجمَّع دون استخراجه وقت الاستعلام. الأعمدة الفارغة تكلّف بتاً في Postgres؛ نموذج بيانات خاطئ يكلّف إعادة كتابة عند وصول طبقة التحليلات (`D5`)

| النوع | الحقول |
|---|---|
| 🍽️ وجبات الطعام | الوجبة · الكمية (الكل/نصف/قليل/رفض) · الوقت · ملاحظة |
| 😴 النوم والغفوات | من · إلى · المدة المحسوبة · جودة النوم |
| 🚼 دورة المياه وتغيير الحفاض | النوع (حفاض/حمام) · الحالة · الوقت |
| 😊 الحالة المزاجية | المزاج · الوقت · ملاحظة |
| 💊 الدواء | الاسم · الجرعة · الوقت · مَن أعطاها |
| 🏥 الصحة العامة | الحرارة · العَرَض · الإجراء المتخذ |
| 📦 طلب مستلزمات | الصنف · الكمية · العجلة |
| 📋 عام | نص حر + مرفق صورة |

- [x] **2.2** API كامل — [care-reports](src/app/api/care-reports/route.ts) · إنشاء وتعديل وحذف ناعم · فلترة بالطفل/الفصل/النوع/التاريخ · **إبلاغ جماعي حتى 60 طفلاً**
- [x] **2.3** [شاشة المعلمة](<src/app/(dashboard)/care/page.tsx>) — الأطفال أولاً ثم النوع: المعلمة تنهي الغداء فتُبلغ عن الستة الذين أكلوا؛ النوع-أولاً يعني إعادة اختيار الأطفال لكل تقرير
- [x] **2.4** إبلاغ جماعي — "الغرفة كلها نامت 12:30–14:00" ملاحظة واحدة عن 15 طفلاً، وطلب كتابتها 15 مرة يضمن ألا تُكتب مرة
- [x] **2.5** رفع الصورة يعمل — [`/api/care-reports/photo`](src/app/api/care-reports/photo/route.ts) منفصل عن `/api/upload` لسبب واحد يهم: **الكائن يُنسب إلى الطفل لا إلى التقرير**، وإلا نجت صورته من تجهيل سجله. `capture` يفتح الكاميرا مباشرة، والصورة تُرفع قبل الحفظ لتراها المعلمة وتزيلها إن أرادت · تظهر مصغَّرة للطاقم وبعرض أكبر في بوابة الأهل
- [x] **2.6** [StudentCareFeed](src/components/care/StudentCareFeed.tsx) في ملف الطفل — الأيام تنازلياً والتقارير داخل اليوم تصاعدياً (اليوم قصة: فطور ثم نوم ثم مزاج)
- [x] **2.7** ملخص يومي بالإيميل 17:00 بتوقيت الرياض — **لولي الأمر لا للطفل**: عائلة بطفلين تصلها رسالة واحدة. `summarizedAt` يمنع الإرسال مرتين
- [x] **2.8** إشعار Push فوري — **لا يحمل محتوى التقرير إطلاقاً**، فقط النوع واسم الطفل: الإشعار يُعرض على شاشة مقفلة ويمرّ ببنية Google أو Huawei، وليس مكاناً لحرارة طفل أو دوائه
- [x] **2.8ب** التجهيل يمسح ملاحظات التقارير وحقول الدواء والأعراض — يبقى الشكل الإحصائي (النوع · الوقت · كم أكل · كم نام)

## الأسبوع 6 — الحضور المتقدم والفئات العمرية

- [x] **2.9** `AgeGroup` بخمس فئات · **مصفوفة على الفصل** لأن حضانة صغيرة تشغّل غرفة واحدة لسنة–3 سنوات ويجب أن تستطيع قول ذلك. `ClassGroup` **بقي** (الاستيراد وشاشة الأنشطة يقرآنه) وموثَّق كـلا-يُستخدم-للجديد · **مُتحقَّق: كل الفصول الخمسة زُرعت من الحقل القديم**
- [x] **2.10** `Class.capacity` — **تحذير لا رفض**: حضانة وافقت على طفل زائد تحتاج النظام أن يسمح، والحظر الصارم يعلّم الطاقم رفع الرقم حتى يسكت. `null` = غير مضبوطة ولا تحذّر (وإلا اشتعلت التحذيرات على مدرسة لم تطلبها)
- [x] **2.11** `Student.attendanceDays` — **`Int[]` على الطفل لا جدول `AttendanceSchedule`**: علاقة واحد-لواحد بحقل مصفوفة واحد، فالجدول المنفصل يضيف ربطاً على أكثر شاشة تُقرأ ولا يشتري شيئاً. فارغة = الأسبوع المعتاد (وإلا صارت المدرسة كلها "غير متوقعة")
- [x] **2.12** `AttendanceStatus` — الطابعان الزمنيان لا يفرّقان بين "غائب" و"لم يسجّل أحد شيئاً بعد"، ووليّ الأمر أمام صف فارغ لا يعرف أيّهما. `NO_RECORD` **لا يُخزَّن** — هو غياب الصف
- [x] **2.13** `3/5 أيام` لكل طفل و`2/3 حاضر` لكل يوم — **المقام أيام تَوقُّع الطفل لا أيام فتح الحضانة**: طفل بثلاثة أيام لم يفوّت شيئاً بغيابه في اليومين الآخرين
- [x] **2.14** [شبكة أسبوعية](src/components/attendance/WeeklyAttendanceGrid.tsx) — الأسئلة أسبوعية (مَن يغيب كل أحد؟ هل يأتي فعلاً ثلاثة أيام؟) وهي غير مرئية يوماً بيوم. خلية غير متوقَّعة تُعرض رمادية لا "غائب"
- [ ] **2.15** منتقي وقت دائري — **مؤجَّل**: `<input type="time">` يعطي منتقي النظام الأصلي على الجوال، وهو أفضل مما نبنيه. يُعاد النظر مع تطبيق Expo (`3.24`)
- [x] **2.16** [مسار الحالة](src/app/api/attendance/students/status/route.ts) — غائب/إجازة/حاضر/حذف السجل، فردي أو جماعي (يوم عطلة = إجراء واحد)
- [x] **2.17** [رسم دائري](src/components/attendance/AttendanceDonut.tsx) في لوحة التحكم — دائري مفرّغ: الرقم المقروء فعلاً في الوسط
- [x] **2.12ب** إصلاح `checkin` — كان `findFirst`+`create`، و`@@unique([studentId, date])` من `0.40` تجعل العودة بعد الانصراف **خطأ 500**. صار `upsert`

## الأسبوع 7 — التقويم والوحدات

- [x] **2.18** `CalendarEvent` بثلاثة أنواع — **نوع واحد كان سيُخفي اختلافها**: الدرس ينتمي لوحدة ويشغل خانة، النشاط تشترك فيه الغرفة، والإعلان **بلا مدة أصلاً** (`endAt` يُسقَط له حتى لو أُرسل)
- [x] **2.19** [التقويم](<src/app/(dashboard)/calendar/page.tsx>) بعروض يوم/أسبوع/شهر — يوم وأسبوع على شبكة ساعات لأن سؤالهما "متى، وهل يتعارض"؛ الشهر خلايا لأن الساعة عندها ضجيج والسؤال "ماذا يحدث هذا الشهر"
- [x] **2.20** فلترة بالفصل والمعلمة — حدث بلا فصول = على مستوى المدرسة **ويظهر رغم الفلتر** (وإلا أخفى الفلتر الإعلانات التي تخص الجميع)
- [x] **2.21** `CalendarEventClass` — جدول ربط صريح كـ`ActivityInvite`
- [x] **2.22** `Unit` + `Lesson` + `UnitFile` — **الملفات جدول منفصل**: وضعها على صف الوحدة يجرّ ميغابايتات إلى كل استعلام يسرد الوحدات · الرفع والحذف يعملان الآن ([`/api/units/:id/files`](<src/app/api/units/[id]/files/route.ts>) + [`UnitFilesPanel`](src/components/units/UnitFilesPanel.tsx)) — لوحة مضمّنة في البطاقة لا صفحة تفصيل: القائمة ورقتا عمل أو ثلاث، وصفحة كاملة لعرض ثلاثة روابط خطوة تنقّل بلا مقابل
- [x] **2.23** [شاشة الوحدات](<src/app/(dashboard)/units/page.tsx>) — نشط/مؤرشف **تبويبان لا فلتر**: نشاطان مختلفان (التخطيط لهذا الفصل ≠ مراجعة العام الماضي) · ترتيب · بحث بتأخير 250ms
- [x] **2.24** `UnitClass` + `CalendarEvent.unitId/lessonId` — الدرس يستنتج وحدته بنفسه فلا يختلف الاثنان
- [x] **2.22ب** `Lesson.orderIndex` صريح — الترتيب بتاريخ الإنشاء ينكسر لحظة إدراج درس بين اثنين. إعادة الترتيب تستقبل التسلسل كاملاً لا حركة from/to

## الأسبوع 8 — الأرشفة والمناوبات والتخزين

- [x] **2.25** الأرشفة — `archivedAt` على **الفصول والوحدات فقط**. الأطفال والطاقم يحملون `status` + `leftAt` من عمل الاحتفاظ، فالمؤرشف لهم = `status != ACTIVE`؛ **عمود رابع متداخل على تلك الجداول كان سيختلف مع الثلاثة الموجودة عاجلاً أو آجلاً**
- [x] **2.26** [إحصائيات مقسّمة](src/app/api/statistics/roster/route.ts) — نشط/مؤرشف/**مجهَّل**/سلة. السؤالان مختلفان ("كم حجمنا" و"كم تاريخاً نحفظ")، وحدود الخطة تحسب النشط فقط
- [x] **2.27** [RecordActions](src/components/ui/RecordActions.tsx) — تعديل · أرشفة · حذف بهذا الترتيب دائماً: الآمن أولاً والنهائي أخيراً وبالأحمر. **الأرشفة والحذف بندان منفصلان** — وعدان مختلفان، وزر واحد يعني الاثنين هو كيف تفقد حضانة سنة حضور وهي تُرتّب غرفة
- [x] **2.28** [المناوبات](<src/app/(dashboard)/shifts/page.tsx>) — **صف لكل شخص/يوم لا قاعدة تكرار**: جدول الحضانة يُتفق عليه أسبوعاً بأسبوع ويتغيّر باستمرار، ونموذج التكرار كان سيقضي وقته كله مُتجاوَزاً
- [x] **2.29** [حساب التخزين](src/lib/storage-usage.ts) + حصة `max_storage_mb` على الخطة (0 = بلا حد، نفس اصطلاح `max_students`) · مُخزَّن مؤقتاً ويُحدَّث ليلياً **بعد** تنظيف السلة لا قبله
- [x] **2.30** [شاشة التخزين](<src/app/(dashboard)/settings/storage/page.tsx>) — شريط مكدّس لا دائرة: السؤال "كم اقتربت من الحد" يجيبه شريط بعرض معلوم ولا تجيبه دائرة · زر الإفراغ **يحذف PDF الفواتير فقط** لأنها الفئة الوحيدة **القابلة لإعادة الإنتاج**؛ صورة طفل لا تُولَّد من جديد فحذفها قرار لكل ملف
- [x] **2.31** `validateTenantUpload` على المسارات الأربعة — يقيس الحجم **بعد** ترميز base64 لأنه ما يُخزَّن فعلاً (قياس الملف الخام يسمح بتجاوز الثلث) · يرجع **507** لا 413: "المساحة نفدت" مشكلة أخرى بحلٍّ آخر

## 🗄️ بنية البيانات والخصوصية — الجزء المؤجَّل

> **القرار (1 أغسطس 2026)**: نُفِّذت "المجموعة الحمراء" فقط قبل M1 لأن تكلفتها تتضاعف بعد دخول بيانات حقيقية.
> ما يلي مؤجَّل **لما بعد M2** — تكلفته ثابتة ولا يزداد بالتأخير، وبعضه **يجب** أن يأتي بعد إصلاح المحرّك المالي.

### D1 — أبعاد تحليلية للمدرسة *(المرحلة 2 من المواصفة)*
- [ ] **D1.1** إضافة أعمدة `regionCode` · `regionName` · `city` · `district` · `latitude` · `longitude` · `capacity` · `licenseType` · `schoolCategory` مع فهارس تحليلية
- [ ] **D1.2** واجهة جمع هذه البيانات في معالج إنشاء المدرسة وإعداداتها
> **لماذا مؤجَّل**: الأعمدة رخيصة في أي وقت — المكلف هو واجهة الجمع، ولا نعرف بعد أي أبعاد تحتاجها التقارير فعلاً.

### D2 — تصنيف الحقول *(المرحلة 5)*
- [ ] **D2.1** إضافة تعليقات `/// PII` · `/// Sensitive` · `/// Operational` · `/// Analytics` لكل حقل في المخطط
- [ ] **D2.2** سكربت يستخرج التصنيف ويولّد سجل معالجة البيانات (Record of Processing)
> **لماذا مؤجَّل**: توثيق بحت بصفر تكلفة ترحيل. يُضاف تدريجياً مع كل تعديل على المخطط.

### D3 — الاحتفاظ والتجهيل ✅ *(نُفِّذ 2 أغسطس 2026)*
- [x] **D3.1** **القرار**: 5 سنوات من `leftAt` للأطفال والموظفين · قابلة للتعديل (3–15) من لوحة الأدمن · السجل المالي يبقى كاملاً بعد إزالة الهوية منه
- [x] **D3.2** `status` · `leftAt` · `retentionUntil` · `anonymizedAt` على `Student` و `Teacher` + `anonymizedAt` على `Guardian`
- [x] **D3.3** خدمة التجهيل — `src/lib/anonymization.ts` · تُبقي الأبعاد التحليلية (العمر بالأشهر · كود الجنسية · سنة التسجيل/المغادرة) وتُشتق قبل إتلاف مصادرها
- [x] **D3.4** وظيفة ليلية `cron/anonymize` (03:00 UTC) + جدول `AnonymizationLog` + شاشة `/admin/data-retention`
- [x] **D3.5** `SystemSettings` (صف واحد) + إعادة احتساب التواريخ عند تغيير المدة
- [x] **D3.6** ربط `NotificationLog` بالطفل/الموظف (`studentId`/`teacherId`) + تنظيف `ActivityLog` عبر `entity_id` — الاسم كان ينجو من التجهيل في كل رسالة وكل سطر سجل
- [x] **D3.7** واجهة "إنهاء تسجيل الطالب": سبب المغادرة (متخرّج/منسحب/منقول) + تاريخها — التاريخ هو ما تُحسب منه مدة الاحتفاظ
- [x] **D3.8** وصل مسارات `cancel` و `reactivate` بالدورة + منع تعديل أو إعادة تفعيل سجل مجهَّل (409)
> **التوثيق**: [`docs/DATA_LIFECYCLE.md`](docs/DATA_LIFECYCLE.md) — الحالات الثلاث · ما يُحذف وما يبقى · الثغرات المعروفة.

- [x] **D3.9** الترحيل مُطبَّق على الإنتاج (2 أغسطس 2026) · `migrate status` = up to date · صف السياسة مزروع 5/5/مفعَّل · 21 طفل و7 موظفين كلهم `ACTIVE` · **التُقط العمر عند التسجيل وسنة التسجيل لكل الـ21** قبل أي إتلاف · 0 مستحق للتجهيل

#### متابعات D3 — بعد التطبيق
- [ ] **D3.10** حذف يدوي لصفوف `NotificationLog` القديمة (قبل الترحيل) — لا تحمل `studentId` فلا يصلها التجهيل. لم تُطابَق بالاسم عمداً: مطابقة ضبابية تمسح سجل عائلة أخرى ما زالت مسجَّلة
- [x] **D3.11** حذف كائنات R2 في التجهيل — [`discardFilesOwnedBy`](src/lib/stored-files.ts) **بعد إغلاق المعاملة لا داخلها**: تراجُع المعاملة مع حذف مُنفَّذ = سجل حيّ يشير إلى ملفات مفقودة، وهو الاتجاه الأسوأ. نجاة كائن واحد تُسجَّل `fatal` ويبقى صفّه فتعيد الكنسة المحاولة
- [x] **D3.12** ✅ [اختبارات `retentionUntil`](tests/data-retention.test.ts) — سنة كبيسة وحدود اليوم بتوقيت الرياض مغطاة — **يحتاج إضافة Vitest، لا يوجد إطار اختبارات في المشروع**. مرتبطة بالمهمة `0.54`
- [x] **D3.13** [واجهة إنهاء خدمة الموظف](<src/app/(dashboard)/teachers/[id]/page.tsx>) — **السبب والتاريخ يُسألان لا يُفترضان**: `leftAt` يبدأ عدّاد الاحتفاظ، فتاريخ مضبوط على «اليوم» لمن غادر في مارس يجدول المحو متأخراً ثمانية أشهر بصمت · «استقالة» مقابل «إنهاء عقد» لا يُسجَّل في أي مكان آخر · [المسار](<src/app/api/teachers/[id]/cancel/route.ts>) صار يقبل `status` و`leftAt` (اختياريان — النداءات القديمة بجسم فارغ تُبقي السلوك السابق) · الشارة تعرض السبب والتاريخ لا «غير نشط» فقط · نفس الزر يعيد التفعيل
- [ ] **D3.14** ذكر مدة الاحتفاظ (5 سنوات) صراحةً في **سياسة الخصوصية** (`P5`) و**اتفاقية معالجة البيانات** — إلزامي حسب PDF المتطلبات
- [ ] **D3.15** مراجعة `D7.4` القانونية: تأكيد المدة + تأكيد خلوّ حقول المؤهلات من معرّفات عرضية قبل أي تقرير قطاعي

### D4 — اللقطات التاريخية *(المرحلة 8)*
- [ ] **D4.1** نموذج `SchoolMonthlySnapshot`: `totalStudents` · `activeStudents` · `totalTeachers` · `occupancyRate` · `averageFee` · `revenue` · `outstandingAmount`
- [ ] **D4.2** وظيفة شهرية تولّد لقطة لكل مدرسة
> ⚠️ **حاجز صلب**: **لا يجوز البدء قبل إتمام الأسبوع 2 (إصلاح المالية).** الإيرادات حالياً مضاعفة والضريبة تُحسب كإيراد ورسوم التسجيل مزدوجة. لقطة شهرية تُجمِّد أرقاماً خاطئة **بلا إمكانية إعادة حساب بأثر رجعي**.

### D5 — طبقة التحليلات *(المرحلة 9)*
- [ ] **D5.1** Schema منفصل `analytics` (يحتاج `multiSchema` في Prisma)
- [ ] **D5.2** Views مجمَّعة: `student_demographics` · `school_capacity` · `payment_statistics`
- [ ] **D5.3** دور قاعدة بيانات للقراءة فقط لا يرى الجداول التشغيلية إطلاقاً
> **ملاحظة تقنية**: دعم Prisma للـ Views والـ multiSchema ما زال preview. الأنسب مبدئياً: materialized views تُدار بـ SQL خام داخل الترحيلات، لا تُنمذَج في Prisma.

### D6 — منع إعادة التعرف *(المرحلة 10)*
- [ ] **D6.1** حد أدنى: لا تُظهر تجميعاً أقل من 10 أطفال أو 5 مدارس → `null` أو `"<10"`
- [ ] **D6.2** ⚠️ **الحدود وحدها لا تكفي** — تُهزم بهجوم الفروقات (استعلام "الرياض" ثم "الرياض عدا حي كذا"). يلزم: تسجيل كل استعلام تحليلي + منع التقسيمات المتداخلة
- [ ] **D6.3** اختبارات تحاول كسر التجهيل عمداً قبل أي إطلاق للتقارير

### D7 — تصحيحات معمارية على المواصفة الأصلية
- [ ] **D7.1** ⚠️ **`sha256(idNumber)` ليس مجهّلاً** — رقم الهوية السعودي 10 أرقام ببنية معروفة (<10⁹ احتمال)، يُكسر بجدول قوس قزح في دقائق. **نُفِّذ بـ HMAC مع pepper سري خارج قاعدة البيانات** ✅
- [ ] **D7.2** ⚠️ **`analyticsId` = pseudonymisation لا anonymisation** — ما دام مخزَّناً بجانب المعرّف الحقيقي فالربط ممكن، وهي بيانات شخصية قانونياً. **ممنوع وصفها بـ"مجهّلة بالكامل" في أي عقد أو وثيقة**
- [ ] **D7.3** خطة تدوير مفتاح التشفير (`PII_ENCRYPTION_KEY`) — كيف نُعيد التشفير دون توقف؟
- [ ] **D7.4** 🔴 **مراجعة قانونية لـ PDPL** قبل أي التزام تعاقدي بإنتاج تقارير قطاعية — بيانات الأطفال فئة حساسة

### D8 — توسيع وثيقة استراتيجية البيانات
- [ ] **D8.1** توسيع `docs/DATA_STRATEGY.md`: بنية Data Warehouse المستقبلية + أمثلة التقارير القطاعية + دورة حياة البيانات التفصيلية

---

## 📧 بند مؤجَّل — ترقية البريد (قبل M2)

**الوضع الحالي**: Gmail شخصي عبر SMTP — يعمل بلا نطاق ولا سجل تجاري.

| | Gmail شخصي *(الآن)* | نطاق + Resend *(عند M2)* |
|---|---|---|
| المتطلبات | لا شيء | نطاق `.com` ~10$/سنة — **لا يحتاج سجلاً تجارياً** |
| الحد | 500 رسالة/يوم | 3000/شهر مجاناً |
| الوصول للبريد الوارد | 🟡 قد يذهب للمهملات أحياناً | 🟢 ممتاز (SPF/DKIM/DMARC) |
| المُرسِل الظاهر لولي الأمر | بريد شخصي | `noreply@حضانتك.com` |

**متى تُنفَّذ**: قبل الأسبوع 9 (M2 — البيع التجاري). كافٍ تماماً حتى ذلك الحين.

**كيف**: شراء نطاق ← تفعيله في Resend ← تعديل متغيرين في Vercel:
```
RESEND_API_KEY = re_xxxxx
FROM_EMAIL     = noreply@نطاقك.com
```
ثم حذف متغيرات `SMTP_*`. **لا يُلمس أي سطر كود** — طبقة البريد تختار المزوّد تلقائياً (Resend له الأولوية عند وجود مفتاحه).

- [ ] **E1** شراء نطاق `.com`
- [ ] **E2** تفعيل النطاق في Resend وضبط سجلات DNS
- [ ] **E3** استبدال متغيرات SMTP بـ Resend في Vercel والمحلي

---

## الأسبوع 9 — بوابة ولي الأمر واللمسات الأخيرة

- [x] **2.32** [بوابة ولي الأمر](src/app/portal/page.tsx) — **خارج `(dashboard)`**: جمهور مختلف بلا قائمة جانبية، وبناؤها داخل تخطيط الطاقم يعني أن كل تغيير للطاقم يهدّد شاشة الأهل. الدخول بالجوال + رمز على البريد، **نفس توكن التطبيق** (البوابة وجه التطبيق على الويب لا منتج ثانٍ). التوكن في `sessionStorage` لا `localStorage` — البوابات تُفتح على هواتف مُعارة
- [x] **2.33** [`/api/portal/me`](src/app/api/portal/me/route.ts) — **استدعاء واحد** لا ستة: تُفتح على جوال عند بوابة الحضانة، وستة طلبات على شبكة ضعيفة = شاشة تتجمّع أمام العين
- [x] **2.34** `StudentGuardian` — الحقول المسطحة كانت **تحمل** ولي أمر ثانٍ ولا **تصفه**: لا صلة قرابة ولا إذن استلام ولا حساب دخول، ولا طريقة لربط جدّة بحفيدين دون كتابتها مرتين. `Student.guardianId` **بقي** كجهة الاتصال الرئيسية · **مُتحقَّق: 21/21 رابط رئيسي، صفر تعارض، صفر ازدواج**
- [x] **2.35** [`formatHijri`](src/lib/datetime.ts) + [DualDate](src/components/ui/DualDate.tsx) — `islamic-umalqura` (أم القرى الرسمي) لا `islamic` المحسوب؛ يختلفان بيوم وهو ما يهمّ على مستند
- [x] **2.36** ثنائية اللغة — **تصحيح تقديري**: ظننتها كنساً لـ200 سلسلة، لكن `en.json` **مكتمل أصلاً** (259 مفتاحاً، صفر ناقص). الناقص كان التوصيل لا الترجمة: `t()` كان يستورد العربي مباشرة. [i18n.ts](src/lib/i18n.ts) + [المزوّد](src/lib/i18n-provider.tsx) + [المبدّل](src/components/layout/LanguageSwitcher.tsx) · 15 ملفاً و316 استدعاءً حُوِّلت آلياً · **التفضيل في كوكي لا localStorage** ليقرأه الخادم قبل أول رسم — وإلا ظهرت الصفحة عربية ثم انقلبت، وقلب RTL→LTR ليس وميضاً بل قفزة تخطيط كاملة · **مُتحقَّق: `<html lang="en" dir="ltr">` و`lang="ar" dir="rtl"` حسب الكوكي**
- [x] **2.37** `BillingCycle` + [billing-cycles.ts](src/lib/billing-cycles.ts) — يومي/أسبوعي/شهري/سنوي/مخصص. الشهور والسنوات تتقدّم **بالتقويم لا بعدد أيام ثابت**، مع تثبيت 31 يناير على 28 فبراير (نفس عيب `0.22`)
- [x] **2.38** [التذكيرات](src/app/api/reminders/route.ts) مفصولة — **ليستا مهمة واحدة**: تذكير الدفع يلاحق مالاً مستحقاً، وتذكير التجديد يسأل عائلة أن تقرّر. دمجهما يعني إرسال "رسومكم متأخرة" لعائلة تسجيلها ينتهي الشهر القادم
- [x] **2.38ب** 🐛 دوال التاريخ في `message-variables` و`utils` — **تصحيح**: `ar-SA` يُرجع التقويم الميلادي فعلاً (لا الهجري كما ظننت أولاً). العيبان الحقيقيان: **بلا منطقة زمنية** (تستخدم منطقة المضيف، وعلى Vercel هي UTC ⇒ كل تاريخ بعد 21:00 بتوقيت الرياض يسمّي أمس)، و**أرقام هندية** `٠٣/٠٨/٢٠٢٦` مقابل `03/08/2026` من `formatAst` — نفس التاريخ يبدو تاريخين. `formatDate` و`formatTime` و`formatCurrency` كلها كانت مصابة
- [x] **2.39** `jobTitle` (نص حرّ باقتراحات) + `educationLevel` (**enum**) + `specialization` — المسمى نص لأن كلمات كل حضانة تختلف و enum يحشر "مساعدة معلمة" في أسوأ خانة تناسبها؛ المؤهل enum لأنه ما يسأل عنه التفتيش وما تُجمَّع عليه التقارير
- [x] **2.39ب** 🐛 **الزرع الأول لم يطابق شيئاً** — بحث عن "بكالوريوس" و"بكالريوس" والبيانات الحيّة تقول **"بكلوريوس"**: إملاء ثالث، وهو بالضبط سبب وجود العمود. ترحيل تصحيحي بأنماط أوسع · **مُتحقَّق: 2 صُنِّفا، و"تربية"/"دراسة 1"/الفارغ تُركت فارغة لا مُخمَّنة**
- [x] **2.40** [Icon](src/components/ui/Icon.tsx) — 19 أيقونة SVG بدل الإيموجي. الإيموجي يرسمه نظام التشغيل، و🚼 و💊 تظهران مربعات رمادية على أندرويد الأقدم وهو أكثر ما يحمله طاقم الحضانات؛ ولا تقبل لوناً فلا تستطيع بلاطة النوع الإشارة إلى حالة. **ليست مكتبة**: 19 رمزاً مقابل مئات الكيلوبايتات
- [x] **2.41** [form-schemas.ts](src/lib/form-schemas.ts) — Zod على النماذج الخمسة الباقية، **مشتركة بين المتصفح والمسار**: مخطط عميل ينحرف عن الخادم يُنتج أسوأ نتيجة — نموذج يقبل ما يرفضه المسار برسالة لم يُصمَّم لعرضها
- [ ] **2.42** استبدال ~300 لون hex — **مؤجَّل**: كنس ميكانيكي واسع بلا أثر وظيفي، والشاشات ما زالت تُضاف. أفضل بعد `M2` مع `2.36`
- [ ] **2.43** ✅ **M2: مراجعة شاملة + جاهزية البيع التجاري**

---

# المرحلة 3 — تطبيق الجوال (الأسابيع 10-13)

## الأسبوع 10 — الأساس

- [ ] **3.1** إنشاء مشروع Expo + TypeScript + هيكل المجلدات
- [ ] **3.2** نظام التصميم: ألوان · خطوط عربية · مكوّنات مشتركة
- [ ] **3.3** **RTL كامل** + مبدّل عربي/إنجليزي
- [ ] **3.4** التنقّل (Expo Router) + شريط سفلي لكل دور
- [ ] **3.5** طبقة API + إدارة الحالة + تخزين آمن للتوكن
- [ ] **3.6** شاشات المصادقة (5): اختيار نوع الحساب · دخول بالجوال · OTP · تعيين كلمة المرور · استعادة
- [ ] **3.7** **تبديل الدور** بين ولي أمر ومعلمة في نفس الحساب

## الأسبوع 11 — تطبيق ولي الأمر (12 شاشة)

- [ ] **3.8** الرئيسية: جدول اليوم + الأنشطة اليومية + إشعارات
- [ ] **3.9** التقويم: شريط أيام أفقي + منتقي شهر/سنة + أحداث اليوم
- [ ] **3.10** ملف الطفل (قراءة) + المعلومات الصحية
- [ ] **3.11** الأوصياء
- [ ] **3.12** سجل الأنشطة (التقارير اليومية مقسّمة بالتاريخ والنوع)
- [ ] **3.13** الملفات (تقييمات · مستندات)
- [ ] **3.14** سجل الحضور
- [ ] **3.15** خطة الاشتراك + الفواتير
- [ ] **3.16** الإشعارات · الملف الشخصي · الإعدادات · سياساتنا · الشروط
- [ ] **3.17** دعم أكثر من طفل في نفس الحساب (مبدّل الأطفال)

## الأسبوع 12 — تطبيق المعلمة (13 شاشة)

- [ ] **3.18** الرئيسية + التقويم
- [ ] **3.19** **الأنشطة**: شبكة الأنواع الثمانية مع زر "إبلاغ"
- [ ] **3.20** نموذج إبلاغ مخصص لكل نوع + رفع صور
- [ ] **3.21** **الفصول**: بحث · تحديد الكل · إلغاء التحديد · تحديد متعدد
- [ ] **3.22** ورقة خيارات الحضور: تسجيل حضور · انصراف · غياب · أجازة
- [ ] **3.23** تسجيل حضور جماعي
- [ ] **3.24** منتقي وقت دائري
- [ ] **3.25** ملف الطفل (قراءة حسب الصلاحية)
- [ ] **3.26** الإشعارات · الملف الشخصي · الإعدادات

## الأسبوع 13 — الإشعارات والتلميع

- [ ] **3.27** ربط Push (FCM + APNs + HMS) + طلب الإذن + التوجيه العميق
- [ ] **3.28** أنواع الإشعارات: تقرير جديد · حضور/انصراف · إعلان · تذكير دفع · حدث تقويم
- [ ] **3.29** تفضيلات الإشعارات لكل مستخدم
- [ ] **3.30** وضع عدم الاتصال (تخزين مؤقت للقراءة)
- [ ] **3.31** ضغط الصور قبل الرفع
- [ ] **3.32** شاشات التحميل والأخطاء والفراغ
- [ ] **3.33** **اختبار على أجهزة حقيقية**: iPhone · Android · Huawei
- [ ] **3.34** أيقونة التطبيق · شاشة البداية · لقطات المتاجر (8 أحجام)
- [ ] **3.35** ✅ **M3: التطبيق جاهز للرفع**

---

# المرحلة 4 — النشر (الأسبوع 14)

- [ ] **4.1** بناء الإصدارات عبر EAS Build
- [ ] **4.2** رفع على App Store Connect + إفصاح الخصوصية (مراجعة مشددة لتطبيقات الأطفال)
- [ ] **4.3** رفع على Google Play + إفصاح أمان البيانات
- [ ] **4.4** رفع على Huawei AppGallery
- [ ] **4.5** الاستجابة لملاحظات المراجعة (**توقّعي رفضاً في المحاولة الأولى**)
- [ ] **4.6** صفحة تحميل موحّدة + رابط في الإيميلات
- [ ] **4.7** ✅ **M4: التطبيق منشور**

---

## المخاطر وخطط التخفيف

| الخطر | الاحتمال | التخفيف |
|---|---|---|
| **بيروقراطية Apple (D-U-N-S)** تعطّل النشر | عالٍ | البدء **اليوم الأول** — المهمة P1 |
| إصلاح المالية يكشف عيوباً أعمق في نموذج البيانات | متوسط | خُصّص يومان (8-9) لا يوم واحد |
| 12 ساعة × 6 أيام لـ14 أسبوعاً غير مستدام | **عالٍ جداً** | يوم راحة كامل نهاية كل أسبوعين — يوفّر أكثر مما يكلّف |
| رفض Apple لتطبيق يخص الأطفال | متوسط | سياسة خصوصية دقيقة + عدم جمع بيانات غير ضرورية + لا إعلانات |
| أولياء الأمور لا يفتحون الإيميل | **عالٍ** | سبب حقيقي لعودة الواتساب بعد M2 — احتفظي بالكود جاهزاً |
| تضخّم النطاق أثناء التنفيذ | عالٍ | أي طلب جديد → قائمة "بعد M4"، لا يدخل الخطة الحالية |
| ترحيل الملفات من base64 إلى R2 يفقد بيانات | منخفض | نسخة احتياطية قبل الترحيل + تشغيل على نسخة اختبار أولاً |

---

## قائمة فحص ما قبل كل معلم

**قبل M1:**
- [ ] كل الثغرات الحرجة العشر مغلقة
- [ ] كل الحسابات المالية صحيحة ومختبَرة
- [ ] النسخ الاحتياطي يعمل والاستعادة مختبَرة
- [ ] المراقبة والتنبيهات مفعّلة
- [ ] لا توجد بيانات ثابتة أو مزيّفة في الواجهة

**قبل M2:**
- [ ] كل فيتشرز ينمو مغطاة
- [ ] بوابة ولي الأمر تعمل على الجوال
- [ ] الصلاحيات مطبَّقة على كل مسار
- [ ] حصص التخزين مفعّلة

**قبل M3:**
- [ ] اختبار على 3 أجهزة حقيقية على الأقل
- [ ] Push يعمل على المنصات الثلاث
- [ ] RTL سليم في كل الشاشات

---

## ملاحظة على النطاق المؤجَّل

**الفوترة الضريبية والربط مع هيئة الزكاة والضريبة (ZATCA) خارج هذه الخطة** بقرار صريح — تبقى الفوترة كما هي حالياً بعد إصلاح أخطائها فقط (المهام 0.70 – 0.77).

⚠️ للعلم: الفوترة الإلكترونية **إلزامية نظاماً** في السعودية. تأجيلها قرار تجاري مقبول للإطلاق، لكنه دَيْن تنظيمي يجب سداده. الجهد المقدَّر لاحقاً: **4-5 أيام** للمرحلة الأولى (رمز QR + الحقول الإلزامية + منع التعديل)، و**2-3 أسابيع** للمرحلة الثانية عبر مزوّد حلول معتمد.
